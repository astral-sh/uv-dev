name = "coordinated-scope-indexes-right"
description = "Shared distributions whose dependency metadata belongs to the later explicit index."

[testgen]
disable = true

[expected]
satisfiable = true

[root]
requires = ["shared"]

[packages.shared.versions."1.0.0"]
requires = ["right-only"]

[packages.shared.versions."2.0.0"]
requires = ["right-new-only"]
