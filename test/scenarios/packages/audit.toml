name = "audit"
description = "Packages used by audit tests that lock projects before querying a mocked advisory service."

[root]
requires = ["iniconfig==2.0.0"]

[expected]
satisfiable = true

[expected.packages]
iniconfig = "2.0.0"

[packages.iniconfig.versions."2.0.0"]
sdist_backend = "in-tree"

[packages.sniffio.versions."1.3.1"]
sdist_backend = "in-tree"

[packages.typing-extensions.versions."4.10.0"]
sdist_backend = "in-tree"

[testgen]
disable = true
