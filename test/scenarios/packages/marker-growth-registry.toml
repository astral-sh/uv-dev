name = "marker-growth-registry"
description = "Shared and platform-conditional dependencies for repeated conflict resolution."

[root]

[expected]
satisfiable = true

[packages.growth-shared.versions."1.0.0"]
requires_python = ">=3.12"
sdist_backend = "in-tree"

[packages.growth-platform.versions."1.0.0"]
requires_python = ">=3.12"
requires = ["growth-shared==1.0.0"]
sdist_backend = "in-tree"

[testgen]
disable = true
