name = "extra-build-match-runtime"
description = "A source distribution without static metadata for runtime-matched build requirements."

[root]
requires = ["dynamic-runtime-source", "runtime-requirement"]

[expected]
satisfiable = true

[packages.dynamic-runtime-source.versions."1.0.0"]
requires_python = ">=3.12"
wheel = false
sdist_backend = "legacy-setuptools"

[packages.runtime-requirement.versions."1.0.0"]
requires_python = ">=3.12"
sdist_backend = "in-tree"

[testgen]
disable = true
